export type userSaveType = {
    email : string | null,
    phoneNumber: string | null,
    uid : string
}

export type todoDataType = {
    todo :string, 
    uid : string,
    email :string,
    completed :boolean
}